server:
  port: 8899

spring:
  application:
    name: toolbox
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

logging:
  level:
    root: INFO
    com.toolbox: DEBUG
